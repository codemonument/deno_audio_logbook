DB Request once for each month (only getting the selected month at a time)

- The user can select a month and year to get the data for
- Add Option to create a custom name for the file, or leave name unchanged and
  add a separate field for the name

  - Add Option for Tags (e.g. "bad mood", "dreamy", "sleepy", "happy" ?)

  - make those tags selectable in the UI?
